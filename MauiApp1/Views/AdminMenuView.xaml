<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SpinningTrainer.Views.AdminMenuView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Administrador">
    <Grid BackgroundColor="{StaticResource PrimaryDark}">

        <StackLayout
            Margin="0,300,0,0"
            HorizontalOptions="Center"
            Spacing="75">

            <Button
                x:Name="btnOpenDataManagment"
                Background="{StaticResource PrimaryDark}"
                Clicked="btnOpenDataManagment_Clicked"
                CornerRadius="20"
                FontSize="26"
                HeightRequest="80"
                HorizontalOptions="Center"
                Text="Gestión de Empresa"
                TextColor="White"
                WidthRequest="300" />
        </StackLayout>

    </Grid>
</ContentPage>